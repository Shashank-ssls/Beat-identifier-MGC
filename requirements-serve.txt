# Minimal deps to SERVE the default (SVM) model in the Docker image.
# Deliberately excludes torch / xgboost / mlflow / panns so the container stays
# small. Versions pinned to match training (so the joblib model unpickles).
fastapi==0.111.1
uvicorn[standard]==0.30.3
python-multipart==0.0.9
numpy==1.26.4
pandas==2.2.2
scikit-learn==1.5.1
joblib==1.4.2
librosa==0.10.2.post1
soundfile==0.12.1
pyyaml==6.0.2
