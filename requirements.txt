# ── Core scientific stack ─────────────────────────────────────────────
numpy==1.26.4
pandas==2.2.2
scipy==1.13.1
pyyaml==6.0.2

# ── Audio / DSP ───────────────────────────────────────────────────────
librosa==0.10.2.post1
soundfile==0.12.1
audioread==3.0.1

# ── Classic ML ────────────────────────────────────────────────────────
scikit-learn==1.5.1
xgboost==2.1.1

# ── Deep learning (CPU/GPU build resolved by the install env) ─────────
# Plain `pip install torch` on Windows = CPU-only. For an NVIDIA GPU install
# the CUDA build, e.g. `--index-url https://download.pytorch.org/whl/cu121`
# (see README). torchaudio is not needed — SpecAugment is hand-rolled.
torch==2.3.1

# ── Pretrained audio embeddings (transfer-learning path) ──────────────
# PANNs CNN14 backbone. Note: panns_inference downloads its checkpoint via
# `wget`; on Windows fetch it manually (see README) — we point it at models/.
panns-inference==0.1.1
torchlibrosa==0.1.0

# ── Experiment tracking ───────────────────────────────────────────────
mlflow==2.14.3
# mlflow 2.14 imports pkg_resources, which setuptools 81+ removed (and Python
# 3.12 may not ship setuptools at all) — pin it so fresh installs/CI work.
setuptools<81

# ── Serving ───────────────────────────────────────────────────────────
fastapi==0.111.1
uvicorn[standard]==0.30.3
python-multipart==0.0.9

# ── Plotting (EDA, confusion matrices) ────────────────────────────────
# Loosely pinned: the exact 3.9.1 pin has no wheel on some platforms and falls
# back to a (failing) source build. Plot versions don't affect results.
matplotlib>=3.8,<4
seaborn>=0.13,<1

# ── Hyperparameter tuning ─────────────────────────────────────────────
optuna==4.9.0

# ── Dev / test / lint ─────────────────────────────────────────────────
pytest==8.3.2
ruff==0.5.5

# ── Desktop packaging (optional; `python build_exe.py`) ───────────────
pyinstaller==6.10.0
